{
  "name": "wildcat-student-app",
  "version": "0.1.0",
  "private": true,
  "description": "Native iOS + Android wrapper of the Wildcat student portal, with Core NFC read/write and camera.",
  "type": "module",
  "scripts": {
    "sync-web": "node scripts/sync-web.mjs",
    "prepare-native": "npm run sync-web && npx cap sync",
    "add:ios": "npx cap add ios",
    "add:android": "npx cap add android",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android",
    "run:ios": "npm run prepare-native && npx cap run ios",
    "run:android": "npm run prepare-native && npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/camera": "^6.1.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/push-notifications": "^6.0.4",
    "@capacitor-community/barcode-scanner": "^4.0.1",
    "@capawesome-team/capacitor-nfc": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
